import * as React from "react";
import * as styles from './styles.scss';
const Calcul = () => {
    const style = styles as any;
    return (
        <div className = {`calculator`}>이것은 계산기 디폴트이다
            <p>안녕하세요 이제 잘되는 군요</p>
        webpack --watch는 편해요</div>
    );
};

export default Calcul;
